[클래스 정의]

class 클래스이름(MusicPlayer) {
	// 속성
	파일이름
	플레이위치
	현재상태(플레이, 정지, 감기)

	// 메소드(행위)
	파워온()
	파워오프()
	플레이()
	정지()
	뒤로감기()
	앞으로감기()
	
	// 값을지정(setter), 참조(getter)
	// 속성값을 지정하고 참조할 수 있는 방법
	
	// 이벤트(상태의 변화를 외부에 통보)
	오류
	성공
	상태
}